> [!IMPORTANT]
> Avoid rewriting published git history — avoid force pushing, or rebasing/amending/squashing commits
> that are already pushed.
>
> Always ensure commits maintain a stable, working build state with no broken imports or type errors.
